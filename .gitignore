# Python
__pycache__/
*.pyc
*.pyo

# Secrets / local state — NEVER commit these
.riot_api_key
.riot_api_key.txt
*.key
*.token

# Local cache + scratch
cache/
*.tmp
*.out
*.log

# local launchers (may contain machine paths)
*.bat

# distribution build artifacts
build/
dist/payload.zip
*.exe

# stale pre-folder-reorg root duplicates (shadowed PyInstaller; archived, not used)
/_attic/

# Copilot accidentally committed the frozen build here; it is a build artifact
dist/SmitelessApp/

# Local agent workspaces
.codex/
.cursor/
thoughts/
